/**
 * Inherits all game logic from SolitaireGame.
 * Moves are made by the player through the GUI.
 */
public class ManualGame extends SolitaireGame {

    public ManualGame(int size, BoardType boardType) {
        super(size, boardType);
    }
}